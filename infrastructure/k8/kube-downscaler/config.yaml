apiVersion: v1
kind: ConfigMap
metadata:
  name: kube-downscaler
data:
  # downscale for non-work hours
  DEFAULT_UPTIME: "Mon-Fri 08:00-21:00 Asia/Kolkata"
  EXCLUDE_NAMESPACES: "flux-system"
  EXCLUDE_DEPLOYMENTS: "coredns,cluster-autoscaler,kube-downscaler"