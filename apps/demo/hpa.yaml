apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: rollouts-demo
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: rollouts-demo
  minReplicas: 1
  maxReplicas: 8
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 95